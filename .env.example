# Copy this file to `.env` and fill in the value. `.env` is gitignored.
#
# Get a key from https://console.cloud.google.com/ — create a project, enable
# the "Web Fonts Developer API", then create an API key under "Credentials".
# The key only needs access to that API; restrict it accordingly.

GOOGLE_FONTS_API_KEY=
